"""SPIDER version - the one place the numbers are written.

Every program and every report reads them from here, so that a version can
never disagree with itself across the files.
"""

VERSION = "2.4.0"                                 # the version of the skill
SCHEMA = 2                                        # the database layout this code writes and expects
